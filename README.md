# Recruiting

Standalone build of the Recruiting app from aschcapital.com.

Extracted from the aschcapital.com single-page site into a self-contained app: a minimal shell (`index.html`) boots the labs runtime directly into the `recruiting` screen. Open `index.html` in a browser (or serve the folder) to run it.
